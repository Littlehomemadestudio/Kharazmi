# نمودار کلاس RASK! — ساختار موجودیت‌ها و روابط

## توضیحات

این نمودار کلاس‌های اصلی سیستم RASK! را با روابط، ویژگی‌ها و متدهای کلیدی نشان می‌دهد. ساختار بر اساس الگوی **DDD** طراحی شده و موجودیت‌ها، اشیاء ارزشی، سرویس‌ها و فرمان‌ها را شامل می‌شود.

### راهنمای خواندن نمودار

- **◄——** ترکیب (Composition): چرخه حیات وابسته؛ والد بدون فرزند معنا ندارد
- **◇——** تجمیع (Aggregation): رابطه ضعیف‌تر؛ فرزند مستقل از والد وجود دارد
- **◁——** وراثت (Inheritance): کلاس فرزند از کلاس والد ارث‌بری می‌کند
- **..>** وابستگی (Dependency): استفاده موقت یا ارجاع

---

## نمودار کلاس‌های هسته دامنه (Core Domain)

```mermaid
classDiagram
    direction TB

    class Project {
        +str name
        +str description
        +datetime created_at
        -dict~str,Task~ _tasks
        -dict~tuple,Dependency~ _deps
        -list~EventListener~ _listeners
        +add_task(task: Task) Task
        +create_task(title: str, **kwargs) Task
        +get_task(task_id: TaskId) Optional~Task~
        +require_task(task_id: TaskId) Task
        +delete_task(task_id: TaskId) void
        +update_task(task_id: TaskId, **changes) void
        +change_status(task_id: TaskId, new_status) void
        +add_dependency(dep: Dependency) Dependency
        +remove_dependency(pred_id, succ_id, dep_type) void
        +dependencies_of(task_id: TaskId) list~Dependency~
        +dependents_of(task_id: TaskId) list~Dependency~
        +tasks() Iterator~Task~
        +dependencies() Iterator~Dependency~
        +roots() list~Task~
        +leaves() list~Task~
        +snapshot() Project
        +to_dict() dict
        +from_dict(data: dict) Project$
        +subscribe(listener: EventListener) void
        -_emit(event: DomainEvent) void
        -_would_create_cycle(pred, succ) bool
        -_can_reach(src, dst) bool
        -_find_path(src, dst) list~TaskId~
    }

    class Task {
        +TaskId id
        +str title
        +str description
        +Duration duration
        +Priority priority
        +TaskStatus status
        +RiskLevel risk
        +Progress progress
        +set~Tag~ tags
        +list~ResourceAllocation~ resources
        +Optional~PertEstimate~ pert
        +Optional~datetime~ earliest_start
        +Optional~datetime~ latest_finish
        +float x
        +float y
        +Optional~datetime~ early_start
        +Optional~datetime~ early_finish
        +Optional~datetime~ late_start
        +Optional~datetime~ late_finish
        +Optional~Slack~ slack
        +advance(new_status: TaskStatus) void
        +set_progress(percent: int) void
        +set_duration(amount: float, unit: DurationUnit) void
        +add_tag(tag: Tag) void
        +remove_tag(tag: Tag) void
        +assign_resource(alloc: ResourceAllocation) void
        +unassign_resource(resource_name: str) void
        +touch() void
        +is_critical: bool
        +is_terminal: bool
        +is_active: bool
        +effective_duration: Duration
        +remaining_duration: Duration
        +to_dict() dict
        +from_dict(data: dict) Task$
    }

    class Dependency {
        <<frozen>>
        +TaskId predecessor_id
        +TaskId successor_id
        +DependencyType type
        +Duration lag
        +key: tuple
        +to_dict() dict
        +from_dict(data: dict) Dependency$
    }

    class TaskId {
        <<frozen Value Object>>
        +str value
        +generate(prefix: str) TaskId$
        +__str__() str
    }

    class Duration {
        <<frozen Value Object>>
        +int minutes
        +hours: float
        +days: float
        +weeks: float
        +of(amount: float, unit: DurationUnit) Duration$
        +as_timedelta() timedelta
        +humanize() str
        +__add__(other: Duration) Duration
        +__sub__(other: Duration) Duration
    }

    class Progress {
        <<frozen Value Object>>
        +int percent
        +is_complete: bool
        +remaining_fraction: float
    }

    class Slack {
        <<frozen Value Object>>
        +Duration total_slack
        +Duration free_slack
        +is_critical: bool
        +is_near_critical: bool
    }

    class PertEstimate {
        <<frozen Value Object>>
        +Duration optimistic
        +Duration most_likely
        +Duration pessimistic
        +expected: Duration
        +std_dev: float
        +variance: float
    }

    class Tag {
        <<frozen Value Object>>
        +str name
        +__str__() str
    }

    class Resource {
        <<frozen Value Object>>
        +str name
        +float capacity_per_day
    }

    class ResourceAllocation {
        <<frozen Value Object>>
        +Resource resource
        +float load
    }

    class TimeWindow {
        <<frozen Value Object>>
        +datetime start
        +datetime end
        +duration: Duration
        +overlaps(other: TimeWindow) bool
        +contains(moment: datetime) bool
    }

    %% روابط
    Project "1" *-- "*" Task : _tasks
    Project "1" *-- "*" Dependency : _deps
    Task "1" *-- "1" TaskId : id
    Task "1" *-- "1" Duration : duration
    Task "1" *-- "1" Progress : progress
    Task "1" o-- "*" Tag : tags
    Task "1" o-- "*" ResourceAllocation : resources
    Task "1" o-- "0..1" PertEstimate : pert
    Task "1" o-- "0..1" Slack : slack
    Dependency "1" *-- "1" TaskId : predecessor_id
    Dependency "1" *-- "1" TaskId : successor_id
    Dependency "1" *-- "1" Duration : lag
    ResourceAllocation "1" *-- "1" Resource : resource
```

---

## نمودار کلاس‌های فرمان (Commands)

```mermaid
classDiagram
    direction TB

    class Command {
        <<abstract>>
        +str name
        +str description
        +execute(project: Project) void*
        +undo(project: Project) void*
        +redo(project: Project) void
    }

    class CreateTaskCommand {
        +str title
        +int duration_minutes
        +Priority priority
        +float x
        +float y
        -str _created_id
        +execute(project) void
        +undo(project) void
    }

    class DeleteTaskCommand {
        +TaskId task_id
        -dict _snapshot
        -list~Dependency~ _deps
        +execute(project) void
        +undo(project) void
    }

    class UpdateTaskCommand {
        +TaskId task_id
        +dict changes
        -dict _old_values
        +execute(project) void
        +undo(project) void
    }

    class MoveTaskCommand {
        +TaskId task_id
        +float new_x
        +float new_y
        -float _old_x
        -float _old_y
        +execute(project) void
        +undo(project) void
    }

    class ChangeStatusCommand {
        +TaskId task_id
        +TaskStatus new_status
        -TaskStatus _old_status
        +execute(project) void
        +undo(project) void
    }

    class AddDependencyCommand {
        +TaskId predecessor_id
        +TaskId successor_id
        +DependencyType dep_type
        +int lag_minutes
        -bool _executed
        +execute(project) void
        +undo(project) void
    }

    class RemoveDependencyCommand {
        +TaskId predecessor_id
        +TaskId successor_id
        +DependencyType dep_type
        +int lag_minutes
        -bool _existed
        +execute(project) void
        +undo(project) void
    }

    class UndoStack {
        -list~Command~ _stack
        -int _index
        -int _limit
        -list~Callable~ _listeners
        +push(command: Command) void
        +execute(command: Command, project) void
        +undo(project) bool
        +redo(project) bool
        +can_undo() bool
        +can_redo() bool
        +clear() void
        +subscribe(listener) void
        +next_undo_name() Optional~str~
        +next_redo_name() Optional~str~
    }

    Command <|-- CreateTaskCommand
    Command <|-- DeleteTaskCommand
    Command <|-- UpdateTaskCommand
    Command <|-- MoveTaskCommand
    Command <|-- ChangeStatusCommand
    Command <|-- AddDependencyCommand
    Command <|-- RemoveDependencyCommand

    UndoStack "1" o-- "*" Command : _stack
```

---

## نمودار کلاس‌های سرویس و الگوریتم

```mermaid
classDiagram
    direction TB

    class TaskService {
        +Project project
        +UndoStack undo
        +SchedulingService scheduling
        +create_task(title, duration_minutes, priority, x, y) Optional~TaskId~
        +delete_task(task_id) void
        +update_task(task_id, **changes) void
        +move_task(task_id, x, y) void
        +change_status(task_id, new_status) void
        +add_dependency(pred_id, succ_id, dep_type, lag) bool
        +remove_dependency(pred_id, succ_id, dep_type) void
        +tasks_sorted_by(key) list~Task~
        +search(query) list~Task~
        +statistics() dict
    }

    class SchedulingService {
        +Project project
        -CPMResult _last_cpm
        -PERTSummary _last_pert
        +recalculate(start_anchor) CPMResult
        +run_pert(start_anchor) PERTSummary
        +run_monte_carlo(iterations, target_minutes, seed) MonteCarloResult
        +level_resources(start_anchor) LevelingResult
        +last_cpm: CPMResult
        +last_pert: PERTSummary
    }

    class AIService {
        +str api_key
        +str model
        +str base_url
        +float temperature
        +int max_tokens
        +generate_route(project_summary, user_goals) Route$
        +generate_step_questions(step) list~StepQuestion~$
        +chat(messages, on_chunk) str$
        +stream_chat(messages, on_chunk) void$
        +save_settings(settings) void$
        +load_settings() dict$
    }

    class ExportService {
        +export_to_csv(project, path) void
        +export_to_json(project, path) void
    }

    class Advisor {
        +analyze(project) list~Insight~
        +suggest_next(project) list~Task~
    }

    class RouteExport {
        +export_route(route, fmt, path) void
    }

    class CPMResult {
        +datetime project_start
        +datetime project_end
        +Duration project_duration
        +list~TaskId~ critical_path
        +CycleError cycle_error
        +ok: bool
    }

    class PERTSummary {
        +Duration expected_duration
        +float std_dev
        +float confidence_85
        +float confidence_95
    }

    class MonteCarloResult {
        +int iterations
        +float mean_duration
        +float std_dev
        +float p50
        +float p85
        +float p95
        +float on_time_probability
    }

    class LevelingResult {
        +CPMResult cpm
        +dict resource_usage
        +list~Conflict~ conflicts
    }

    TaskService --> UndoStack : uses
    TaskService --> SchedulingService : uses
    TaskService --> Project : modifies
    SchedulingService --> Project : reads
    SchedulingService --> CPMResult : produces
    SchedulingService --> PERTSummary : produces
    SchedulingService --> MonteCarloResult : produces
    SchedulingService --> LevelingResult : produces
    AIService ..> Project : reads
```

---

## نمودار کلاس‌های زیرسیستم تقویم

```mermaid
classDiagram
    direction TB

    class CalendarStore {
        -dict~str,Calendar~ _calendars
        -dict~str,Event~ _events
        -list~StoreListener~ _listeners
        +add_calendar(calendar) Calendar
        +create_calendar(name, color, description) Calendar
        +get_calendar(cal_id) Optional~Calendar~
        +update_calendar(cal_id, **changes) void
        +set_calendar_visible(cal_id, visible) void
        +delete_calendar(cal_id) void
        +calendars() Iterator~Calendar~
        +visible_calendars() Iterator~Calendar~
        +add_event(event) Event
        +create_event(cal_id, title, start, end) Event
        +get_event(event_id) Optional~Event~
        +update_event(event_id, **changes) void
        +delete_event(event_id) void
        +events() Iterator~Event~
        +events_in_range(start, end) list~Event~
        +events_on_day(day) list~Event~
        +upcoming_events(days) list~Event~
        +search(query) list~Event~
        +subscribe(listener) void
        +to_dict() dict
        +from_dict(data) CalendarStore$
    }

    class Calendar {
        +str id
        +str name
        +str color
        +str description
        +bool is_default
        +bool visible
        +create(name, color, description) Calendar$
        +to_dict() dict
        +from_dict(data) Calendar$
    }

    class Event {
        +str id
        +str calendar_id
        +str title
        +str description
        +str location
        +datetime start
        +datetime end
        +bool all_day
        +EventType event_type
        +EventStatus status
        +Optional~str~ color
        +Optional~RecurrenceRule~ recurrence
        +list~Attendee~ attendees
        +list~Reminder~ reminders
        +bool completed
        +move_to(new_start) void
        +set_time(start, end) void
        +overlaps(start, end) bool
        +is_recurring: bool
        +duration: timedelta
        +to_dict() dict
        +from_dict(data) Event$
    }

    class RecurrenceRule {
        +str frequency
        +int interval
        +Optional~int~ count
        +Optional~datetime~ until
        +expand(event_start, range_start, range_end) Iterator~datetime~
    }

    class CalendarModel {
        +CalendarStore store
        +calendars() list~Calendar~
        +visible_calendars() list~Calendar~
        +events_on_day(shamsi) list~Event~
        +events_in_shamsi_range(start, end) list~Event~
        +events_in_month(year, month) list~Event~
        +events_in_week(containing) list~Event~
        +day_events(shamsi) DayEvents
        +compute_timed_layout(events) list~EventLayout~
        +month_grid(year, month) list
        +create_event(cal_id, title, start, end) Event
        +update_event(event_id, **changes) void
        +delete_event(event_id) void
        +move_event(event_id, new_start) void
        +resize_event(event_id, new_end) void
        +search(query) list~Event~
    }

    class CalendarController {
        +CalendarModel model
        +SelectionManager selection
        +CalendarViewKind view_kind
        +ShamsiDate nav_date
        +Signal view_changed
        +Signal date_changed
        +Signal events_changed
        +set_view(kind) void
        +go_today() void
        +go_next() void
        +go_prev() void
        +go_to_date(d) void
        +create_event_at(start, end, title, all_day) Event
        +create_event_from_nl(text) Event
        +move_event(event_id, new_start) void
        +resize_event(event_id, new_end) void
        +delete_event(event_id) void
        +toggle_event_completed(event_id) void
    }

    class DayEvents {
        +ShamsiDate date
        +list~Event~ all_day
        +list~Event~ timed
        +list~EventLayout~ timed_layout
    }

    class EventLayout {
        +Event event
        +float left
        +float width
        +int column
        +int total_columns
    }

    CalendarStore "1" *-- "*" Calendar : _calendars
    CalendarStore "1" *-- "*" Event : _events
    Event "1" o-- "0..1" RecurrenceRule : recurrence
    CalendarModel "1" *-- "1" CalendarStore : store
    CalendarController "1" *-- "1" CalendarModel : _model
    DayEvents "1" o-- "*" EventLayout : timed_layout
    EventLayout "1" *-- "1" Event : event
```

---

## نمودار کلاس‌های ماندگاری (Persistence)

```mermaid
classDiagram
    direction TB

    class SQLiteRepository {
        +Path db_path
        -threading.RLock _lock
        -sqlite3.Connection _conn
        +upsert_project(project) str
        +list_projects() list~dict~
        +delete_project(project_id) void
        +save_snapshot(project, kind) int
        +load_latest(project_id) Optional~Project~
        +load_snapshot(snapshot_id) Optional~Project~
        +list_snapshots(project_id, limit) list~SnapshotInfo~
        +close() void
        -_init_schema() void
    }

    class CalendarRepository {
        +Path db_path
        -threading.RLock _lock
        -sqlite3.Connection _conn
        +save(store, kind) int
        +load_latest() Optional~CalendarStore~
        +has_snapshot() bool
        +close() void
        -_init_schema() void
    }

    class SnapshotInfo {
        +int id
        +str project_id
        +datetime saved_at
        +str kind
    }

    class JournalStore {
        +Path path
        +list~JournalEntry~ entries
        +add(entry) void
        +search(query) list~JournalEntry~
        +save() void
        +load() void
        +__len__() int
    }

    SQLiteRepository "1" ..> Project : serializes
    CalendarRepository "1" ..> CalendarStore : serializes
    SQLiteRepository "1" o-- "*" SnapshotInfo : returns
```

---

## نمودار رویدادهای دامنه (Domain Events)

```mermaid
classDiagram
    direction TB

    class DomainEvent {
        <<frozen>>
        +datetime occurred_at
    }

    class TaskCreated {
        +TaskId task_id
        +str title
    }

    class TaskUpdated {
        +TaskId task_id
        +str field
        +Any old
        +Any new
    }

    class TaskDeleted {
        +TaskId task_id
    }

    class TaskStatusChanged {
        +TaskId task_id
        +str old
        +str new
    }

    class DependencyAdded {
        +TaskId predecessor_id
        +TaskId successor_id
        +str dep_type
    }

    class DependencyRemoved {
        +TaskId predecessor_id
        +TaskId successor_id
    }

    class CycleDetected {
        +tuple attempted_edge
        +tuple cycle
    }

    class ProjectReset {}

    class ProjectLoaded {
        +str source
        +int task_count
    }

    class ScheduleRecalculated {
        +int project_duration_minutes
        +tuple critical_path
        +datetime recalculated_at
    }

    DomainEvent <|-- TaskCreated
    DomainEvent <|-- TaskUpdated
    DomainEvent <|-- TaskDeleted
    DomainEvent <|-- TaskStatusChanged
    DomainEvent <|-- DependencyAdded
    DomainEvent <|-- DependencyRemoved
    DomainEvent <|-- CycleDetected
    DomainEvent <|-- ProjectReset
    DomainEvent <|-- ProjectLoaded
    DomainEvent <|-- ScheduleRecalculated
```

---

## نمودار شمارش‌ها (Enums)

```mermaid
classDiagram
    direction LR

    class TaskStatus {
        <<enumeration>>
        DRAFT
        READY
        ACTIVE
        BLOCKED
        DONE
        CANCELLED
        DEFERRED
    }

    class Priority {
        <<enumeration>>
        TRIVIAL = 0
        LOW = 1
        MEDIUM = 2
        HIGH = 3
        CRITICAL = 4
    }

    class DependencyType {
        <<enumeration>>
        FINISH_START = FS
        FINISH_FINISH = FF
        START_START = SS
        START_FINISH = SF
    }

    class RiskLevel {
        <<enumeration>>
        NEGLIGIBLE
        LOW
        MEDIUM
        HIGH
        SEVERE
    }

    class DurationUnit {
        <<enumeration>>
        MINUTE
        HOUR
        DAY
        WEEK
    }

    class EventType {
        <<enumeration>>
        NORMAL
        TASK
        REMINDER
        HOLIDAY
    }

    class CalendarViewKind {
        <<enumeration>>
        MONTH
        WEEK
        DAY
        YEAR
    }
```
