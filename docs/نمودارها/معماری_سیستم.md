# معماری سیستم RASK! — لایه‌های DDD

## توضیحات

این نمودار معماری لایه‌ای سیستم RASK! را بر اساس الگوی **طراحی راهنمایی‌شده با دامنه (Domain-Driven Design)** نشان می‌دهد. سیستم از ۶ لایه مجزا تشکیل شده که هر لایه فقط به لایه‌های زیرین خود وابسته است. این جداسازی تضمین می‌کند که منطق کسب‌وکار مستقل از زیرساخت فنی بماند و قابلیت آزمون‌پذیری و نگهداری کد افزایش یابد.

### ساختار لایه‌ها

| لایه | مسئولیت | ماژول پایتون |
|------|---------|-------------|
| **رابط کاربری (UI)** | نمایش اطلاعات، تعامل کاربر، ویجت‌های PySide6 | `kharazmi.ui` |
| **سرویس‌ها (Services)** | هماهنگی عملیات، تراکنش‌های اپلیکیشن | `kharazmi.services` |
| **فرمان‌ها (Commands)** | عملیات برگشت‌پذیر (Undo/Redo) | `kharazmi.commands` |
| **الگوریتم‌ها (Algorithms)** | CPM، PERT، مونت‌کارلو، تسطیح منابع | `kharazmi.algorithms` |
| **هسته دامنه (Core Domain)** | موجودیت‌ها، اشیاء ارزشی، رویدادها | `kharazmi.core` |
| **ماندگاری (Persistence)** | ذخیره‌سازی SQLite، سریال‌سازی | `kharazmi.persistence` |

---

## نمودار معماری

```mermaid
graph TB
    subgraph UI["🖥️ لایه رابط کاربری — UI Layer"]
        direction LR
        MainWindow["RaskMainWindow"]
        Views["Views<br/>GanttView · KanbanView · TimelineView<br/>DashboardView · SimulationView<br/>AIPlannerView · JournalView"]
        CalendarUI["Calendar UI<br/>MonthView · WeekView · DayView · YearView"]
        Dialogs["Dialogs<br/>TaskEditor · NodeEdit · AISettings<br/>ProjectSettings · EventEditor"]
        Widgets["Widgets<br/>Toolbar · InspectorPanel · MiniMap<br/>AIChatPanel · CommandPalette"]
    end

    subgraph Services["⚙️ لایه سرویس‌ها — Services Layer"]
        direction LR
        TaskSvc["TaskService<br/>مدیریت عملیات وظایف"]
        SchedSvc["SchedulingService<br/>هماهنگی CPM/PERT/مونت‌کارلو"]
        ExportSvc["ExportService<br/>خروجی گرفتن از داده‌ها"]
        AdvisorSvc["Advisor<br/>مشاوره هوشمند"]
        RouteExport["RouteExport<br/>خروجی مسیر برنامه‌ریزی"]
    end

    subgraph Commands["🔄 لایه فرمان‌ها — Commands Layer"]
        direction LR
        UndoStack["UndoStack<br/>پشته برگشت/اجرای مجدد"]
        CreateCmd["CreateTaskCommand"]
        DeleteCmd["DeleteTaskCommand"]
        UpdateCmd["UpdateTaskCommand"]
        MoveCmd["MoveTaskCommand"]
        StatusCmd["ChangeStatusCommand"]
        AddDepCmd["AddDependencyCommand"]
        RemDepCmd["RemoveDependencyCommand"]
    end

    subgraph Algorithms["🧮 لایه الگوریتم‌ها — Algorithms Layer"]
        direction LR
        CPM["Critical Path<br/>محاسبه مسیر بحرانی"]
        PERT["PERT<br/>تخمین سه‌نقطه‌ای"]
        MC["Monte Carlo<br/>شبیه‌سازی مونت‌کارلو"]
        RL["Resource Leveling<br/>تسطیح منابع"]
        TopoSort["Topological Sort<br/>مرتب‌سازی توپولوژیکی"]
        CycleDet["Cycle Detection<br/>تشخیص چرخه"]
    end

    subgraph Core["💎 لایه هسته دامنه — Core Domain"]
        direction LR
        Project["Project<br/>ریشه تجمیعی پروژه"]
        Task["Task<br/>موجودیت وظیفه"]
        Dependency["Dependency<br/>وابستگی بین وظایف"]
        VOs["Value Objects<br/>TaskId · Duration · Progress<br/>Slack · PertEstimate · Tag<br/>Resource · TimeWindow"]
        Events["Domain Events<br/>TaskCreated · TaskUpdated<br/>DependencyAdded · CycleDetected<br/>ScheduleRecalculated"]
        Enums["Enums<br/>TaskStatus · Priority<br/>DependencyType · RiskLevel"]
    end

    subgraph Persistence["💾 لایه ماندگاری — Persistence Layer"]
        direction LR
        SQLiteRepo["SQLiteRepository<br/>ذخیره پروژه در SQLite"]
        CalRepo["CalendarRepository<br/>ذخیره تقویم در SQLite"]
        Serializers["Serializers<br/>تبدیل به/از JSON"]
    end

    subgraph AISub["🤖 زیرسیستم هوش مصنوعی"]
        direction LR
        AIService["AIService<br/>اتصال به GLM API"]
        JournalStore["JournalStore<br/>ذخیره یادداشت‌ها"]
    end

    subgraph CalSub["📅 زیرسیستم تقویم"]
        direction LR
        CalStore["CalendarStore<br/>مخزن تقویم و رویدادها"]
        CalModel["CalendarModel<br/>مدل شمسی‌محور"]
        CalController["CalendarController<br/>کنترل‌کننده تقویم"]
        CalEvent["Event<br/>موجودیت رویداد"]
        Recurrence["RecurrenceRule<br/>قوانین تکرار"]
    end

    %% وابستگی‌های بین لایه‌ای
    UI --> Services
    UI --> CalSub
    UI --> AISub

    Services --> Commands
    Services --> Algorithms
    Services --> Core

    Commands --> Core
    Commands --> UndoStack

    Algorithms --> Core

    Core --> Persistence
    CalSub --> Persistence
    AISub --> Core

    Persistence --> SQLiteDB[("🗄️ SQLite Database<br/>rask.sqlite3<br/>calendar.sqlite3")]
    AIService --> GLMAPI[("🌐 GLM API<br/>api.z.ai")]

    %% سبک‌های گرافیکی
    style UI fill:#4A90D9,stroke:#2C5F8A,color:#fff
    style Services fill:#50B86C,stroke:#2D7A3E,color:#fff
    style Commands fill:#F5A623,stroke:#C07D12,color:#fff
    style Algorithms fill:#9B59B6,stroke:#6C3483,color:#fff
    style Core fill:#E74C3C,stroke:#B03A2E,color:#fff
    style Persistence fill:#7F8C8D,stroke:#566573,color:#fff
    style AISub fill:#1ABC9C,stroke:#148F77,color:#fff
    style CalSub fill:#E67E22,stroke:#BA6914,color:#fff
    style SQLiteDB fill:#34495E,stroke:#1C2833,color:#fff
    style GLMAPI fill:#34495E,stroke:#1C2833,color:#fff
```

---

## قوانین وابستگی

```mermaid
graph LR
    UI["رابط کاربری"] -->|"فراخوانی"| Services["سرویس‌ها"]
    Services -->|"استفاده"| Commands["فرمان‌ها"]
    Services -->|"اجرای"| Algorithms["الگوریتم‌ها"]
    Commands -->|"تغییر"| Core["هسته دامنه"]
    Algorithms -->|"خواندن/نوشتن"| Core
    Core -->|"سریال‌سازی"| Persistence["ماندگاری"]

    style UI fill:#4A90D9,color:#fff
    style Services fill:#50B86C,color:#fff
    style Commands fill:#F5A623,color:#fff
    style Algorithms fill:#9B59B6,color:#fff
    style Core fill:#E74C3C,color:#fff
    style Persistence fill:#7F8C8D,color:#fff
```

### اصل وابستگی معکوس

- لایه‌های بالاتر به لایه‌های پایین‌تر وابسته‌اند، نه برعکس
- هسته دامنه (Core) هیچ وابستگی به لایه‌های بالاتر ندارد
- الگوریتم‌ها فقط از موجودیت‌های هسته استفاده می‌کنند
- سرویس‌ها هماهنگ‌کننده بین لایه‌ها هستند
- رابط کاربری فقط از طریق سرویس‌ها با دامنه ارتباط دارد

---

## جریان یک عملیات نمونه: ایجاد وظیفه

```mermaid
sequenceDiagram
    actor User as کاربر
    participant UI as رابط کاربری
    participant TS as TaskService
    participant US as UndoStack
    participant Cmd as CreateTaskCommand
    participant Proj as Project
    participant SS as SchedulingService
    participant CPM as CPM Algorithm
    participant DB as SQLiteRepository

    User->>UI: کلیک «وظیفه جدید»
    UI->>TS: create_task(title, duration)
    TS->>Cmd: ساخت CreateTaskCommand
    TS->>Cmd: execute(project)
    Cmd->>Proj: add_task(task)
    Proj-->>Cmd: شناسه وظیفه
    TS->>US: push(command)
    TS->>SS: recalculate()
    SS->>CPM: run_cpm(project)
    CPM-->>SS: CPMResult
    SS-->>Proj: ScheduleRecalculated event
    Proj-->>UI: بروزرسانی نمایش
    UI->>DB: save_snapshot(project)
    DB-->>DB: ذخیره در SQLite
```
