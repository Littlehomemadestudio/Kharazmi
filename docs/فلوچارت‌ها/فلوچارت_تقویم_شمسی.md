# فلوچارت تقویم شمسی RASK!

## توضیحات
این فلوچارت عملکرد بخش تقویم شمسی برنامه RASK! را نشان می‌دهد. شامل تبدیل تاریخ میلادی به شمسی، ایجاد رویدادها، تنظیم تکرار، و نمایش‌های مختلف تقویم می‌باشد.

```mermaid
flowchart TD
    START([ورود به تب تقویم]) --> CAL_INIT[مقداردهی اولیه تقویم]
    CAL_INIT --> TODAY[محاسبه تاریخ امروز شمسی]
    TODAY --> CONVERT[تبدیل میلادی → شمسی]

    CONVERT --> JALALI_LIB[استفاده از کتابخانه jdatetime]
    JALALI_LIB --> YEAR{سال کبیسه؟}
    YEAR -->|بله| LEAP[اعمال اسفند ۳۰]
    YEAR -->|خیر| NORMAL[تقویم عادی]
    LEAP --> RENDER_CAL
    NORMAL --> RENDER_CAL

    RENDER_CAL[رندر تقویم ماهانه] --> VIEW_MODE{نوع نمایش}

    VIEW_MODE -->|ماهانه| MONTH_VIEW[نمایش ماهانه - QGridLayout]
    VIEW_MODE -->|هفتگی| WEEK_VIEW[نمایش هفتگی - QScrollArea]
    VIEW_MODE -->|روزانه| DAY_VIEW[نمایش روزانه - QListWidget]
    VIEW_MODE -->|لیست| LIST_VIEW[نمایش لیست رویدادها]

    MONTH_VIEW --> DAY_CELL[ساخت Cell روز]
    WEEK_VIEW --> DAY_CELL
    DAY_VIEW --> DAY_CELL
    LIST_VIEW --> EVENT_LIST

    DAY_CELL --> HAS_EVENT{رویداد موجود؟}
    HAS_EVENT -->|بله| EVENT_DOT[نمایش نقطه رویداد]
    HAS_EVENT -->|خیر| EMPTY_CELL[سلول خالی]
    EVENT_DOT --> EVENT_COUNT{تعداد رویدادها}
    EVENT_COUNT -->|۱| SINGLE_DOT[نقطه تکی]
    EVENT_COUNT -->|۲+| MULTI_DOT[نقطه‌های متعدد]
    SINGLE_DOT --> CLICK_WAIT
    MULTI_DOT --> CLICK_WAIT
    EMPTY_CELL --> CLICK_WAIT

    CLICK_WAIT[انتخاب کاربر] --> CLICK_TYPE{نوع کلیک}

    CLICK_TYPE -->|کلیک ساده| SELECT_DAY[انتخاب روز]
    CLICK_TYPE -->|دابل کلیک| NEW_EVENT[ایجاد رویداد جدید]
    CLICK_TYPE -->|کلیک راست| CONTEXT_MENU[منوی زمینه]

    SELECT_DAY --> SHOW_EVENTS[نمایش رویدادهای روز]
    SHOW_EVENTS --> EVENT_ACTION{عمل روی رویداد}

    CONTEXT_MENU -->|رویداد جدید| NEW_EVENT
    CONTEXT_MENU -->|رفتن به امروز| TODAY_NAV[ناوبری به امروز]
    CONTEXT_MENU -->|رفتن به تاریخ| DATE_PICKER[انتخاب تاریخ شمسی]

    NEW_EVENT --> EVENT_FORM[فرم ایجاد رویداد]
    EVENT_FORM --> TITLE_INPUT[وارد کردن عنوان]
    TITLE_INPUT --> DATE_INPUT[انتخاب تاریخ شمسی]
    DATE_INPUT --> TIME_INPUT[انتخاب زمان]
    TIME_INPUT --> DESC_INPUT[توضیحات]
    DESC_INPUT --> CATEGORY[انتخاب دسته‌بندی]
    CATEGORY --> CAT_COLOR[تخصیص رنگ دسته‌بندی]
    CAT_COLOR --> RECURRENCE{تکرار رویداد؟}

    RECURRENCE -->|بدون تکرار| SINGLE_EVENT[رویداد تکی]
    RECURRENCE -->|تکرار| REC_TYPE{نوع تکرار}

    REC_TYPE -->|روزانه| DAILY_REC[تکرار روزانه]
    REC_TYPE -->|هفتگی| WEEKLY_REC[تکرار هفتگی]
    REC_TYPE -->|ماهانه| MONTHLY_REC[تکرار ماهانه]
    REC_TYPE -->|سالانه| YEARLY_REC[تکرار سالانه]
    REC_TYPE -->|سفارشی| CUSTOM_REC[الگوی سفارشی RRule]

    DAILY_REC --> REC_END
    WEEKLY_REC --> REC_END
    MONTHLY_REC --> REC_END
    YEARLY_REC --> REC_END
    CUSTOM_REC --> REC_END

    REC_END[پایان تکرار] --> END_TYPE{نوع پایان}
    END_TYPE -->|تعداد| OCC_COUNT[تعداد تکرارها]
    END_TYPE -->|تاریخ| END_DATE[تاریخ پایان شمسی]
    END_TYPE -->|بی‌نهایت| NO_END[بدون پایان]

    OCC_COUNT --> SAVE_EVENT
    END_DATE --> SAVE_EVENT
    NO_END --> SAVE_EVENT
    SINGLE_EVENT --> SAVE_EVENT

    SAVE_EVENT[ذخیره رویداد در SQLite] --> GENERATE_INST[تولید نمونه‌های تکرار]
    GENERATE_INST --> UPDATE_CAL[به‌روزرسانی تقویم]
    UPDATE_CAL --> RENDER_CAL

    EVENT_ACTION -->|ویرایش| EDIT_EVENT[فرم ویرایش رویداد]
    EVENT_ACTION -->|حذف| DELETE_CONFIRM{تأیید حذف؟}
    EVENT_ACTION -->|تغییر وضعیت| TOGGLE_STATUS[تغییر وضعیت انجام]

    DELETE_CONFIRM -->|بله| DELETE_TYPE{نوع حذف}
    DELETE_CONFIRM -->|خیر| CLICK_WAIT

    DELETE_TYPE -->|تکی| DELETE_ONE[حذف این نمونه]
    DELETE_TYPE -->|همه| DELETE_ALL[حذف تمام تکرارها]
    DELETE_TYPE -->|بعدی| DELETE_FUTURE[حذف از این به بعد]

    DELETE_ONE --> UPDATE_CAL
    DELETE_ALL --> UPDATE_CAL
    DELETE_FUTURE --> UPDATE_CAL

    EDIT_EVENT --> SAVE_EVENT
    TOGGLE_STATUS --> SAVE_EVENT

    EVENT_LIST[نمایش لیست رویدادها] --> FILTER{فیلتر کردن}
    FILTER -->|دسته‌بندی| CAT_FILTER[فیلتر بر اساس دسته]
    FILTER -->|تاریخ| DATE_FILTER[فیلتر محدوده تاریخ]
    FILTER -->|وضعیت| STATUS_FILTER[فیلتر انجام/انجام‌نشده]
    FILTER -->|بدون فیلتر| SHOW_ALL[نمایش همه]

    CAT_FILTER --> SORT_EVENTS
    DATE_FILTER --> SORT_EVENTS
    STATUS_FILTER --> SORT_EVENTS
    SHOW_ALL --> SORT_EVENTS

    SORT_EVENTS[مرتب‌سازی رویدادها] --> RENDER_CAL

    TODAY_NAV --> RENDER_CAL
    DATE_PICKER --> DATE_SEL[انتخاب تاریخ شمسی]
    DATE_SEL --> NAV_TO[ناوبری به تاریخ انتخاب‌شده]
    NAV_TO --> RENDER_CAL

    style START fill:#4CAF50,color:#fff
    style SAVE_EVENT fill:#2196F3,color:#fff
    style RENDER_CAL fill:#FF9800,color:#fff
    style RECURRENCE fill:#FFC107,color:#000
    style VIEW_MODE fill:#FFC107,color:#000
    style CLICK_TYPE fill:#FFC107,color:#000
    style DELETE_TYPE fill:#FFC107,color:#000
    style END_TYPE fill:#FFC107,color:#000
    style YEAR fill:#FFC107,color:#000
    style HAS_EVENT fill:#FFC107,color:#000
    style EVENT_COUNT fill:#FFC107,color:#000
    style FILTER fill:#FFC107,color:#000
```

## توضیح اجزای اصلی

1. **تبدیل تاریخ شمسی**: با استفاده از کتابخانه `jdatetime` تاریخ میلادی به شمسی تبدیل می‌شود و سال کبیسه بررسی می‌گردد.
2. **نمایش‌های مختلف**: تقویم قابلیت نمایش ماهانه، هفتگی، روزانه و لیستی را دارد.
3. **مدیریت رویدادها**: ایجاد، ویرایش، حذف و تغییر وضعیت رویدادها پشتیبانی می‌شود.
4. **تکرار رویدادها**: پشتیبانی از تکرار روزانه، هفتگی، ماهانه، سالانه و الگوی سفارشی RRule.
5. **فیلتر و مرتب‌سازی**: رویدادها قابل فیلتر بر اساس دسته‌بندی، تاریخ و وضعیت می‌باشند.
