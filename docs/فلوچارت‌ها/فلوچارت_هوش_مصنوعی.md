# فلوچارت سرویس هوش مصنوعی RASK!

## توضیحات
این فلوچارت عملکرد سرویس هوش مصنوعی برنامه RASK! را نشان می‌دهد. شامل ارسال درخواست به API مدل GLM، دریافت پاسخ Streaming، پارس کردن پاسخ و نمایش آن در رابط کاربری می‌باشد.

```mermaid
flowchart TD
    START([ورود به تب AI]) --> AI_INIT[مقداردهی اولیه AI Chat]
    AI_INIT --> LOAD_HISTORY[بارگذاری تاریخچه چت از SQLite]
    LOAD_HISTORY --> RENDER_HISTORY[نمایش پیام‌های قبلی]
    RENDER_HISTORY --> CHECK_API_KEY{کلید API موجود؟}

    CHECK_API_KEY -->|خیر| PROMPT_KEY[درخواست وارد کردن کلید API]
    CHECK_API_KEY -->|بله| READY[آماده دریافت ورودی]

    PROMPT_KEY --> KEY_INPUT[کاربر کلید را وارد می‌کند]
    KEY_INPUT --> KEY_VALIDATE{کلید معتبر؟}
    KEY_VALIDATE -->|بله| SAVE_KEY[ذخیره کلید در QSettings]
    KEY_VALIDATE -->|خیر| KEY_ERROR[نمایش خطای کلید نامعتبر]
    KEY_ERROR --> PROMPT_KEY
    SAVE_KEY --> READY

    READY --> USER_INPUT[کاربر پیام تایپ می‌کند]
    USER_INPUT --> INPUT_TYPE{نوع ورودی}

    INPUT_TYPE -->|متن ساده| TEXT_MSG[پیام متنی]
    INPUT_TYPE -->|فرمان پروژه| CMD_MSG[فرمان مدیریتی]
    INPUT_TYPE -->|درخواست تحلیل| ANALYSIS_MSG[درخواست تحلیل پروژه]

    TEXT_MSG --> PREPARE_REQ
    CMD_MSG --> PARSE_CMD[پارس فرمان]
    ANALYSIS_MSG --> GATHER_DATA[جمع‌آوری داده‌های پروژه]

    PARSE_CMD --> CMD_TYPE{نوع فرمان}
    CMD_TYPE -->|ایجاد وظیفه| CMD_TASK[فرمان ایجاد وظیفه]
    CMD_TYPE -->|تحلیل مسیر بحرانی| CMD_CPM[فرمان تحلیل CPM]
    CMD_TYPE -->|پیشنهاد برنامه| CMD_PLAN[فرمان برنامه‌ریزی]

    CMD_TASK --> PREPARE_REQ
    CMD_CPM --> PREPARE_REQ
    CMD_PLAN --> PREPARE_REQ

    GATHER_DATA --> BUILD_CONTEXT[ساخت Context از داده‌ها]
    BUILD_CONTEXT --> PREPARE_REQ

    PREPARE_REQ[آماده‌سازی درخواست] --> BUILD_PROMPT[ساخت Prompt]
    BUILD_PROMPT --> SYSTEM_PROMT[افزودن System Prompt]
    SYSTEM_PROMT --> CONTEXT_INJECT[تزریق Context پروژه]
    CONTEXT_INJECT --> HISTORY_INJECT[افزودن تاریخچه مکالمه]
    HISTORY_INJECT --> USER_MSG_FINAL[افزودن پیام کاربر]
    USER_MSG_FINAL --> API_CALL[ارسال به GLM API]

    API_CALL --> API_REQUEST[POST /chat/completions]
    API_REQUEST --> STREAM_MODE{حالت Streaming؟}

    STREAM_MODE -->|بله| STREAM_START[شروع دریافت Stream]
    STREAM_MODE -->|خیر| FULL_RESPONSE[دریافت پاسخ کامل]

    STREAM_START --> CHUNK_RECV[دریافت chunk]
    CHUNK_RECV --> CHUNK_PARSE[پارس SSE chunk]
    CHUNK_PARSE --> DELTA_CHECK{delta موجود؟}

    DELTA_CHECK -->|بله| EXTRACT_TEXT[استخراج متن delta]
    DELTA_CHECK -->|خیر| DONE_CHECK{stream پایان یافت؟}

    EXTRACT_TEXT --> APPEND_UI[افزودن به نمایش در UI]
    APPEND_UI --> CHUNK_RECV

    DONE_CHECK -->|بله| STREAM_COMPLETE[Stream کامل شد]
    DONE_CHECK -->|خیر| CHUNK_RECV

    FULL_RESPONSE --> PARSE_RESPONSE[پارس پاسخ JSON]
    STREAM_COMPLETE --> PARSE_RESPONSE

    PARSE_RESPONSE --> RESPONSE_CHECK{پاسخ موفق؟}

    RESPONSE_CHECK -->|خطا| ERROR_TYPE{نوع خطا}
    RESPONSE_CHECK -->|موفق| CONTENT_EXTRACT

    ERROR_TYPE -->|خطای شبکه| NET_ERR[خطای اتصال به اینترنت]
    ERROR_TYPE -->|خطای API| API_ERR[خطای API - بررسی کد وضعیت]
    ERROR_TYPE -->|خطای Rate Limit| RATE_ERR[خطای محدودیت نرخ]
    ERROR_TYPE -->|خطای Token| TOKEN_ERR[خطای اتمام Token]

    NET_ERR --> RETRY_CHECK{تلاش مجدد؟}
    API_ERR --> RETRY_CHECK
    RATE_ERR --> WAIT_RETRY[انتظار و تلاش مجدد]
    TOKEN_ERR --> TRUNCATE[کوتاه کردن Context]

    WAIT_RETRY --> RETRY_CHECK
    TRUNCATE --> RETRY_CHECK

    RETRY_CHECK -->|بله - حداکثر ۳ بار| API_CALL
    RETRY_CHECK -->|خیر| SHOW_ERROR[نمایش خطا به کاربر]
    SHOW_ERROR --> READY

    CONTENT_EXTRACT[استخراج محتوای پاسخ] --> CONTENT_TYPE{نوع محتوا}

    CONTENT_TYPE -->|متن ساده| PLAIN_TEXT[متن ساده]
    CONTENT_TYPE -->|Markdown| MD_PARSE[پارس Markdown]
    CONTENT_TYPE -->|کد| CODE_BLOCK[بلوک کد]
    CONTENT_TYPE -->|جدول| TABLE_PARSE[پارس جدول]

    PLAIN_TEXT --> RENDER_MSG
    MD_PARSE --> MD_RENDER[رندر Markdown با QTextEdit]
    MD_RENDER --> RENDER_MSG
    CODE_BLOCK --> SYNTAX_HL[برجسته‌سازی سینتکس]
    SYNTAX_HL --> RENDER_MSG
    TABLE_PARSE --> TABLE_RENDER[رندر جدول HTML]
    TABLE_RENDER --> RENDER_MSG

    RENDER_MSG[نمایش پیام در چت] --> SAVE_MSG[ذخیره پیام در SQLite]
    SAVE_MSG --> UPDATE_TOKENS[به‌روزرسانی شمارنده Token]
    UPDATE_TOKENS --> READY

    style START fill:#4CAF50,color:#fff
    style API_CALL fill:#2196F3,color:#fff
    style STREAM_START fill:#FF9800,color:#fff
    style RENDER_MSG fill:#9C27B0,color:#fff
    style SAVE_MSG fill:#607D8B,color:#fff
    style CHECK_API_KEY fill:#FFC107,color:#000
    style KEY_VALIDATE fill:#FFC107,color:#000
    style INPUT_TYPE fill:#FFC107,color:#000
    style ERROR_TYPE fill:#FFC107,color:#000
    style STREAM_MODE fill:#FFC107,color:#000
    style DELTA_CHECK fill:#FFC107,color:#000
    style DONE_CHECK fill:#FFC107,color:#000
    style RESPONSE_CHECK fill:#FFC107,color:#000
    style RETRY_CHECK fill:#FFC107,color:#000
    style CONTENT_TYPE fill:#FFC107,color:#000
    style CMD_TYPE fill:#FFC107,color:#000
```

## توضیح اجزای اصلی

1. **مدیریت کلید API**: کلید GLM API در QSettings ذخیره و اعتبارسنجی می‌شود.
2. **ساخت Prompt**: System Prompt، Context پروژه و تاریخچه مکالمه ترکیب می‌شوند.
3. **Streaming**: پاسخ API به صورت SSE (Server-Sent Events) دریافت و به‌صورت زنده نمایش داده می‌شود.
4. **مدیریت خطا**: خطاهای شبکه، API، Rate Limit و Token به‌صورت جداگانه مدیریت می‌شوند.
5. **پارس محتوا**: پاسخ بر اساس نوع (متن، Markdown، کد، جدول) پردازش و رندر می‌شود.
6. **ذخیره‌سازی**: تمام پیام‌ها در SQLite ذخیره و شمارنده Token به‌روز می‌شود.
